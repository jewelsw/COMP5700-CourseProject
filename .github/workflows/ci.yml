name: Data Mining CI

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main ]

jobs:
  test:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v3
    
    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.10'
    
    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt
    
    - name: Run linting
      run: |
        pip install flake8
        flake8 src/ --max-line-length=100 --ignore=E501
    
    - name: Run tests
      run: |
        pytest tests/ -v
    
    - name: Validate CSV structure
      run: |
<<<<<<< HEAD
        python -m src.validate_outputs
=======
        python -m src.validate_outputs
>>>>>>> d9865667ddef41688f705e0f6765e4abcd91a0b9
